const router = require('express').Router();

// router.get("/test",(req,res) => {
//     res.send("The user test api calling...")
// });

// router.post("/userCheck",(req,res) => {
//     const { username } = req.body;
//     res.send(`Your username is ${username}`);
// });

module.exports = router;